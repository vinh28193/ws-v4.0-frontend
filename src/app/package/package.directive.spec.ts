import { PackageDirective } from './package.directive';

describe('PackageDirective', () => {
  it('should create an instance', () => {
    const directive = new PackageDirective();
    expect(directive).toBeTruthy();
  });
});
