import { NotificationBackgroundPipe } from './notification-background.pipe';

describe('NotificationBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
