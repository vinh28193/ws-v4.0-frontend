import { CurrencyStorePipe } from './currency-store.pipe';

describe('CurrencyStorePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyStorePipe();
    expect(pipe).toBeTruthy();
  });
});
