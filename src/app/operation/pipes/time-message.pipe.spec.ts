import { TimeMessagePipe } from './time-message.pipe';

describe('TimeMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
