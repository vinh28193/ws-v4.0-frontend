import { MarkSupportingPipe } from './mark-supporting.pipe';

describe('MarkSupportingPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkSupportingPipe();
    expect(pipe).toBeTruthy();
  });
});
